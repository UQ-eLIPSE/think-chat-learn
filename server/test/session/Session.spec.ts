import * as mocha from "mocha";
import { assert } from "chai";

import { Session } from "../../js/session/Session";

describe("/server/js/session/Session", function() {

    describe("constructor", function() {

        it("should produce objects of type `Session`");

        it("should return the same object when an Session object with same ID exists");

    });

    describe(".GetActiveSessions()", function() {

        it("should return all active sessions");

    });

    describe(".GetActiveSessionIds()", function() {

        it("should return all active session IDs");

    });

    describe("#getId()", function() {

        it("should return the ID of the object");

    });



});
